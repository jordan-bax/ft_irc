
// #ifndef TEMPORARY_HPP
// #define TEMPORARY_HPP

// #include "../classes/client.hpp"

// std::vector<client*> get_clients(env &server_env);
// client	*search_client_nick(env &server_env, std::string nick_name);
// bool	nick_available(env &server_env, std::string nick_name);

// bool	channel_exists(env &server_env, std::string name);
// channel *search_channel(env &server_env, std::string name);
// channel	*new_channel(env &server_env, std::string name, client *creator, std::string key = "");
// channel *join_channel(env &server_env, std::string name, client *client, std::string key = "");

// #endif
